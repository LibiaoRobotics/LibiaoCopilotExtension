# 更新日志

## 1.2.0

### 新增

- **用户核心记忆与工程防线最佳实践面板**：
  - 配置面板全新集成用户核心记忆（User Preferences Memory）管理，支持一键注入工程思想钢印、AI 把关体检、目录直达与自动备份保护。
  - 支持随机趣味昵称池切换，换昵称后自动聚焦输入框并定位光标闪烁，支持回车键快捷保存。
  - 昵称输入框样式优化，采用高亮绿色加粗放大视觉呈现。
  - 支持组织指令（`default.instructions.md`）冲突排查、一键清理与只读锁定防篡改。
  - 支持附加记忆（Custom Memory）状态同步与结构化诊断。

## 1.1.0

### 新增

- **Gemini 原生 API 协议链路支持**：原生支持 Google Gemini API（含 `gemini-3.7-flash` 等），支持思考过程展示、多模态识图与工具调用，模型测试工具同步支持原生端点诊断。

### 修复

- **Gemini Schema 400 错误根治**：废除脆弱的黑名单清洗机制，改用 Google OpenAPI 3.0 规范严格白名单，彻底解决 VS Code/MCP 工具定义中非常规 JSON Schema 关键字导致的 400 Bad Request 报错。
- **Gemini 多轮工具调用思考签名保护**：完善 Gemini 多轮对话中工具调用元数据与思考签名（`thoughtSignature`）回传，保证工具链上下文连续性。

### 改进

- **多协议会话性能统计精准化**：会话性能统计（SessionStats）支持按协议模式（Gemini/OpenAI/Anthropic）精准适配流速与 Token 用量统计，状态栏悬停信息更加准确。
- **测试覆盖率全面提升**：补齐多协议流式解析、消息转换、模式兼容性与端到端回归测试套件。

## 1.0.10

### 新增

- **新增内置模型 gemini-3.7-flash**：Gemini 3.7 Flash 思考/多模态模型，1M 上下文（`1048576`），默认 512K，最大 64K 输出，支持多模态识图与工具调用，走 Responses 协议链路。

## 1.0.9

### 新增

- **会话生成性能统计**：悬停状态栏可见统计——按模型累计请求次数、输出 token（含思考）、思考占比、流式耗时、平均速度，数值基于服务端精确值；统计随窗口生命周期累计，不随会话切换重置。

### 改进

- **状态栏占位与进度条**：初始显示「已加载」对勾提示；请求后显示「Libiao Copilot + 进度条 + 百分比」。
- **状态栏 tooltip 紧凑化**：上下文用量与会话统计改为紧凑文本行（数字等宽、按阈值显示告警图标），用量百分比跟随所选上下文大小。
- **内置模型显示名规范化**：全部内置模型增加中文显示名，视觉图标由 `vision` 字段统一驱动。

### 修复

- **正文换行保留**：部分网关把段落分隔符作为独立空白块发送时，空白不再被整块吞掉（标题/列表黏连、加粗失效）。
- **Anthropic 链路工具调用丢失**：模型只回工具调用时不再报「Sorry, no response was returned.」。
- **服务端工具自愈**：模型自发调用未声明工具（`file_search` 等）导致空响应或正文截断时，自动转为错误结果回传并提示改用可用工具。
- **重复工具卡**：网关把上一个调用的参数串台时，不再连续显示两张相同工具卡。
- **思考泄漏进正文**：思考尾巴与裸 `</think>` 标签不再混入正文（极少数误报回合正文头部延迟 1-3 秒）。

## 1.0.8

### 新增

- **模型推荐标记**：模型列表成本列支持自定义备注（如「推荐」「不推荐」），用户配置优先。
- **视觉模型图标可配置**：🖼️/👁️ 二选一，默认图片，各展示点统一。
- **提交信息模型独立配置**：`libiaoCopilot.commitModel`（默认 `deepseek-v4-flash`），不再把模型列表写入 settings.json。
- **「获取模型列表」按钮**：一键清空旧版本留下的模型快照，回落到内置模型 + API 拉取。

### 改进

- **修复视觉能力误判**：API 不返回模态信息时，以内置表声明的 `vision` 值为准。
- **上下文压缩触发优化**：旧消息只剩 1 条时不再发起摘要，直接静默硬截断。

## 1.0.7

### 新增

- **新增内置模型 deepseek-v4-flash-vision-exp**：DeepSeek 实验性视觉模型，1M 上下文 / 384K 输出 / 支持图片输入，走 Responses 链路。

## 1.0.6

### 新增

- **配置面板顶部新增「保存全局配置」按钮**，长表单免滚动到底。
- **零配置即可生成提交信息**：内置 `deepseek-v4-flash` 默认用于 Git 提交信息生成。

### 界面汉化与简化

- 配置面板全面汉化（含弹窗、导入导出）。
- 隐藏「Model Management」区块，供应商管理改为只读，避免误改配置。

## 1.0.5

### 新增

- **新增内置模型 glm-5.3**（智谱最新旗舰）：1M 上下文 / 128K 输出 / 强制思考，走 Anthropic 链路。

## 1.0.4

### 内部维护

- 精简 CHANGELOG，清空历史 lint 报错（无功能变化）。

## 1.0.3

### 修复

- **修复 Responses API 回复内容重复一倍**：部分网关在正文末尾多发一个空增量，触发全文兜底重发；现空增量直接跳过，兜底仅在没有增量时触发。
- 修正 `logLevel` 描述中的日志目录路径。

## 1.0.2

### 新增

- **上下文管理生效**：按「Context Size」档位执行摘要压缩（默认）或硬截断兜底；`libiaoCopilot.contextManagement` 可设为 `off` 完全关闭。

### 修复

- **VSIX 缺失运行时依赖导致激活崩溃**：1.0.1 打包产物未含 `node_modules`，重新打包补齐。

## 1.0.1

### 改进

- **Anthropic 端点简化**：路径直接拼接 `/messages`，`/v1` 前缀由 baseUrl 携带（已配置的 baseUrl 需补上 `/v1`）。

### 修复

- 添加模型时自动选中默认上下文大小，配置菜单不再无选中项。

### 移除

- 移除模型健康检查功能及其三个配置项。

## 1.0.0

首个正式版本，基于 [OAI Compatible Provider for Copilot](https://github.com/JohnnyZ93/oai-compatible-copilot) 构建，面向立镖机器人内部网关与 Copilot Chat 场景。

### 模型发现与配置

- 模型实时发现（与供应商 `/models` 核对，新增自动出现、失效自动移除）、TTL 缓存（默认 10 分钟）、未配置 baseUrl 时不拉取、全部端点失败时显示占位条目。

### 公司模型开箱即用

- 内置 `qwen3.8-max-preview`、`qwen3.8-max`、`deepseek-v4-pro`、`deepseek-v4-flash` 四个模型元数据（1M 上下文、上下文档位、思考强度、视觉能力）。
- 原生 Copilot 配置菜单选择上下文大小与思考强度。

### 本地化与品牌

- 命令、配置说明、README 全部中文化；Git 提交信息默认简体中文；公司 logo 与品牌标识。

### 继承自上游

- OpenAI / OpenAI Responses / Ollama / Anthropic / Gemini 五种 API 模式；视觉模型、工具调用、思考链展示；多供应商管理与同模型多配置；可视化配置界面；API 错误自动重试、请求限速、自定义请求头；状态栏 token 用量统计；一键生成提交信息。

---

> 上游项目完整历史见 [oai-compatible-copilot CHANGELOG](https://github.com/JohnnyZ93/oai-compatible-copilot/blob/main/CHANGELOG.md)。本文件从 1.0.0 起独立维护。
